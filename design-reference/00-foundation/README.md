# Foundation source gate

The catalogue shows a warm light surface system, navy editorial display type, a saturated blue
primary action, green success/verified state, coral attention state, thin neutral borders, and
subtle warm elevation. The perspective composites do not supply exact token values or usable assets.

Do not add `colors.png`, `typography.png`, `spacing.png`, or `components.png` until source exports
or a Figma token handoff provides authoritative values. Their absence is intentional and blocks
visual-fidelity approval; no inferred or generic palette is allowed.
